# Translation-unit audit

`tools/scripts/audit_tu_candidates.py` finds contiguous manifest entries where
the later entry directly calls a function defined by the immediately preceding
entry. It is evidence for review, not evidence of an original source boundary.

## Verified main units

| Range | Unit | Evidence |
| --- | --- | --- |
| `0x23B5C..0x241A0` | `gpu/Gpu_SetupSprites` | Sprite setup, ordering-table enqueue, and status-icon emission form one battle-status renderer. They share the active draw slot, sprite primitive pool, ordering-table table, and `-G8` profile; reconciling `AddPrim` to its `u32 *` API preserves the combined 1604-byte object exactly. |
| `0x620D0..0x621E4` | `psyq/libgpu/tim` | The five contiguous helpers share the PSX TIM container. `Gpu_LoadTimImage` now records the verified TIM header and block layout: flags at `0x04`, an optional CLUT block, and `length + RECT + payload` image blocks. Its typed image and CLUT uploads preserve the complete 276-byte object exactly. |
| `0x64F74..0x65238` | `gpu/libgpu_sys` | Seven contiguous functions match the known Psy-Q `libgpu/sys.c` tail in SDK source order. Their combined object retains every function's retail size; the sole changed relocation is the now object-local `SetDefDrawEnv` → `GetVideoMode` call. `make check` is retail-identical. |
| `0x65528..0x659E4` | `psyq/libgpu/sys_display` | `SetDispMask`, `DrawSync`, rectangle validation, clear-image encoders, and VRAM transfer helpers are contiguous Psy-Q `libgpu/sys.c` APIs. They share GCC 2.8.1 `-mno-split-addresses`, the GPU callback table, debug state, and image packet path; the combined 1212-byte object remains retail-identical. |
| `0x66444..0x664E8` | `gpu/packet_init` | Three adjacent draw-mask and draw-mode packet initializers use the same `GpuCmdPacket` header and command words. The combined object preserves all 164 `.text` bytes exactly and has a host behavioral test; `make check` is retail-identical. |
| `0x664E8..0x66950` | `gpu/drawenv` | The contiguous front and back DRAWENV encoders share their compiler profile and packet construction helpers. They also confirm that GPU state offsets `0x04` and `0x06` are the display width and height. The combined object preserves all 1128 `.text` bytes exactly; `make check` is retail-identical. |
| `0x66970..0x66AA0` | `gpu/drawarea` | The paired top-left and bottom-right draw-area packet encoders share the same `-O1` profile, state dimensions, and clamp algorithm. Their combined object preserves all 304 `.text` bytes exactly; `make check` is retail-identical. |
| `0x66E64..0x67320` | `gpu/vram_transfer` | Adjacent VRAM upload and readback operations share the rectangle clamp, GP0/GP1 readiness protocol, CPU prefix, and 16-word DMA block transfer. The combined object preserves all 1212 `.text` bytes exactly, removes seven duplicate declarations, and passes the separate upload and readback behavioral tests; `make check` is retail-identical. |
| `0x67944..0x67BD0` | `gpu/dma_queue` | Queue initialization and draining operate on one contiguous DMA controller block at `0x80095850`: register pointers, queue indices, saved interrupt mask, and timeout state. The reconciled `GpuDmaState` records that layout; direct field access changes GCC 2.8.1 scheduling, so the matching object retains its established symbol aliases. The combined object preserves all 652 `.text` bytes exactly; `make check` is retail-identical. |
| `0x67DE8..0x68200` | `gpu/dma_transfer` | Four contiguous DMA transfer operations share the retry loop, DMA state, callback installation, and GPU dispatch table. The combined object preserves all 1048 `.text` bytes exactly and removes duplicate controller declarations; `make check` is retail-identical. |
| `0x68264..0x682BC` | `gpu/GetTPage` | The paired libgpu texture-page and CLUT identifier encoders occur with one compiler-emitted `nop` between them. Representing that anonymous `.text` word in the shared source preserves the complete 88-byte object exactly. |
| `0x68304..0x68364` | `gpu/Gpu_SetDither` | The paired packet-bit encoders set dither and draw-enable in the same byte of the GPU command. Each function is followed by two compiler-emitted `nop` words; keeping both anonymous `.text` fragments in the shared source preserves the complete 96-byte object exactly. |
| `0x68364..0x684B4` | `gpu/SetPolyF3` | Ten contiguous GPU primitive initializers set packet length and opcode fields, ending with `SetDrawMode`. The first nine carry three compiler-emitted `nop` words and the last one one word; preserving that padding in the shared source recovers the complete 336-byte primitive-family object exactly. |
| `0x684B4..0x684F4` | `gpu/Gpu_AppendPacketPrimitive` | The packet-length updater appends a primitive when the combined length remains below 17. Its two trailing compiler-emitted `nop` words belong to the same 64-byte object and are retained in the owner source. |
| `0x68228..0x68254` | `gpu/GPU_memset` | The byte-fill helper's two trailing `nop` words are compiler padding, retained with the 44-byte implementation. |
| `0x68254..0x68264` | `gpu/GPU_cw` | The A0:49 BIOS trampoline and its trailing `nop` form one 16-byte object. |
| `0x62EB4..0x62F14` | `psyq/libapi` BIOS trampolines | `InitHeap`, `FlushCache`, `_bu_init`, and the event wrappers are individual 16-byte BIOS-call objects; each owns one trailing compiler `nop`. |
| `0x62F34..0x62FB4` | `psyq/libapi` file BIOS trampolines | The `open` through `erase` wrappers are likewise individual 16-byte B0-call objects with their trailing `nop` retained by the owner source. |
| `0x63264..0x63274` | `psyq/libapi/firstfile` | The B0:42 file-search trampoline and its `nop` form one 16-byte object. |
| `0x76014..0x76180` | `psyq/libapi/counter` | Five contiguous functions match public Psy-Q `libapi/counter.c` in SDK source order. Their combined object retains every function's retail size; only jumps to each function's own epilogue become object-local relocations. `make check` is retail-identical. |
| `0x64494..0x64628` | `psyq/libapi/intr_callbacks` | Ten contiguous callback facade functions are grouped in public Psy-Q `etc/intr.c` in the same order. Reconciling their conflicting recovered callback prototypes to an unprototyped function pointer preserves all 404 `.text` bytes exactly; `make check` is retail-identical. |
| `0x64CD4..0x64F70` | `psyq/libapi/intr_dma` | Public Psy-Q `etc/intr_dma.c` orders `startIntrDMA`, `trapIntrDMA`, `setIntrDMA`, and `memclrIntrDMA` exactly as this range. The intervening 384-byte asm was previously misnamed as a renderer; its DMA interrupt dispatch, bus-error handling, handler table, and diagnostic strings identify it as `trapIntrDMA`. The compiled helpers preserve all 284 `.text` bytes exactly; the initializer's handler table is correctly typed as eight function pointers, and `make check` is retail-identical. |
| `0x64BB4..0x64CD4` | `psyq/libapi/intr_vsync` | Public Psy-Q `etc/intr_vb.c` orders `startIntrVSync`, `trapIntrVSync`, `setIntrVSync`, and `memclr` exactly as this range. The 108-byte intervening asm increments the VSync count and dispatches its eight callbacks, identifying it as `trapIntrVSync`; the compiled helpers preserve all 168 `.text` bytes exactly and share one callback-table type, `void (*[8])(void)`. |
| `0x6E7E0..0x6E848` | `psyq/libapi/pad_control` | The two contiguous entry points implement Psy-Q `StartPAD` and `StopPAD`: `StartPAD` starts BIOS pad polling, disables automatic clearing, enables callbacks, and returns one; `StopPAD` reverses that lifecycle and clears the shared pad-init flag. The Psy-Q library reference identifies `StartPAD` as a `long StartPAD(void)` libapi entry point. |
| `0x68B44..0x68DD0` | `psyq/libgte/average` | Six adjacent vector-average API functions form a complete Psy-Q GTE family in API order. Their combined object preserves all 652 `.text` bytes exactly; `make check` is retail-identical. |
| `0x69834..0x69A40` | `psyq/libgte/pipeline` | Eleven adjacent pipeline API functions occur in the same order in public Psy-Q library implementations. Their combined object preserves all 524 `.text` bytes exactly; `make check` is retail-identical. |
| `0x6C2C0..0x6C3FC` | `psyq/libcd/bios` | The volume and interrupt initializers occur consecutively in public Psy-Q `libcd/bios.c`. Their shared register declarations and compiler flags produce an object whose 316 `.text` bytes are identical to the two prior objects; `make check` is retail-identical. |
| `0x6ABCC..0x6ACD0` | `psyq/libcd/sys` | Seven contiguous CD API wrappers occur in public Psy-Q `libcd/sys.c` in this source order. The combined 260-byte object changes only the two relocations for its own `CdComstr` and `CdIntstr` epilogues; `make check` is retail-identical. |
| `0x6B08C..0x6B2B4` | `psyq/libcd/sys_control` | The sector-read wrappers, DMA data callback, synchronization wrapper, and BCD position converters are contiguous `libcd/sys.c` APIs with the same compiler profile. The combined 552-byte object preserves all preceding code and remains retail-identical. |
| `0x6DED0..0x6E2E0` | `psyq/libspu/spu_transfer` | Four contiguous DMA transfer functions occur in public Psy-Q `libspu/spu.c` in this source order. Their common `SpuRegs` map names the Psy-Q `trans_addr` and `spucnt` registers at `0x1A6` and `0x1AA`; the same typed fields also cover the adjacent DMA-completion handler. The 1040-byte combined object changes only seven calls that become object-local relocations; `make check` is retail-identical. |
| `0x6E2E0..0x6E508` | `psyq/libspu/spu_register_write` | The RXX write/read helpers, priority and delay register controls, and wait loop are contiguous Psy-Q `libspu/spu.c` APIs. Reconciling `_spu_RXX` as the shared `SpuRegs *` model exposes byte-offset addressing explicitly in `_spu_FgetRXXa`; the combined 552-byte object remains retail-identical. |
| `0x72CB4..0x72D14` | `psyq/libds/dscb` | Four adjacent stream start, sync, ready, and DMA-data callback registration helpers form the complete local callback block; the latter three occur in public Psy-Q `libds/dscb.c` in this source order. The combined object preserves all 96 `.text` bytes exactly; `make check` is retail-identical. |
| `0x72574..0x7265C` | `psyq/libds/dsread_control` | The adjacent asynchronous-read initializer and break helper share the `libds` read-controller state and compiler profile. `DsAsyncReadState` now names its nine words, including the saved sync/ready callbacks and final `active` field; a named view remains anchored at the retail `g_DsReadBusy` symbol, preserving all 232 `.text` bytes exactly. |
| `0x757E4..0x75A90` | `akao/spu_transfer` | The timeout check, transfer callback lifecycle, read/write preparation, wait loop, and sample-block upload are one contiguous transfer controller. They share `g_SpuTransferStatus`, and every direct call stays object-local in the recovered unit. The combined 684-byte object preserves the retail executable exactly. |
| `0x81754..0x818A0` | `akao/seq_op9` | The no-op sequence opcode is a direct forwarding wrapper for the preceding `SeqOp_StopVoice`. Reconciling its formerly unprototyped one-argument call with the shared `(AkaoTrack *, u32)` ABI retains both passed registers and preserves all 332 bytes exactly. |
| `0x79A18..0x79C14` | `akao/voice_envelopes` | The envelope update loop directly removes ended voices from the immediately preceding 24-entry voice-bank helper. The established `AkaoTrack` layout explains its `0x11C` stride, `assigned_voice_index` at `0xF0`, and the second bank at `+0x1AA0`; its matching byte-view code remains in place until the typed form also preserves the 508-byte object. |
| `0x7ECE8..0x7F134` | `akao/voice_pitch` | The two adjacent loop-range selectors and their pitch dispatcher share the `AkaoTrack` voice state. Reconciling the former `void *` definition with its caller proves that both thresholds are unsigned `u32`; making the upper declaration signed changes the generated comparison and fails retail matching. The merged 1,100-byte object remains retail-identical. |
| `0x32970..0x32C64` | `save/Save_LoadCardFileIntoRuntime` | Memory-card read setup, the cancellation callback, and the following load/CRC flow form one controller. The successful CRC branch registers the adjacent `Save_CancelUiFlow` callback, and the merged 756-byte object remains retail-identical. |
| `0x27DB4..0x27DE0` | `task/Task_MovementFlags` | Three consecutive task-script flag writers share the `-G8` compiler profile and the movement/collision globals `D_8009CED0` and `D_8009CED4`. The recovered 44-byte control unit retains its exact manifest layout and `make check` is retail-identical. |
| `0x3D14..0x4188` | `task/Task_PointMotion` | The point-turn and point-movement commands share the active node’s target/turn-speed cache, current entity, and `Gte_Atan2` heading calculation. Their exact 1140-byte `-G8` object retains the distinct turn-only and velocity-producing paths; `make check` is retail-identical. |
| `0x39E8..0x3BE8` | `task/Task_NodeActions` | Child-node allocation and the action-wait command operate on the same `TaskNode` root and current entity. Their common `TaskNode *` free-list and pool view removes conflicting integer declarations while preserving the exact 512-byte `-G8` object; `make check` is retail-identical. |
| `0x2DE0..0x2F00` | `task/Task_DrawSyncAndFlush` | The contiguous scene-map flush and task-node free-list initializer share the `-G8` compiler profile and both occur in GPU pipeline setup. The following allocator switches to ASPSX 2.21 with `--dont-expand-li`, marking the recovered 288-byte unit boundary; `make check` is retail-identical. |
| `0x7640..0x77E0` | `task/Task_BeginSceneTransition` | Consecutive equip-screen and save/load launch tasks share the same `-G8` pending-node controller: mark the node, defer by 12 bytes, enter the scene-load transition, run the target menu, and clear the pending state. The recovered 416-byte unit is retail-identical. |
| `0x340EC..0x3430C` | `menu/Menu_CreateInventoryTabView` | The inventory-tab constructor directly invokes the following bonus-point allocation constructor after establishing its widgets and context flags. Reconciling the call from `M2C_UNK` to its real `void` return recovers the shared 544-byte UI setup unit; `make check` is retail-identical. |
| `0x37840..0x379BC` | `menu/Menu_DrawWeaponModSlots` | The equip-options list wrapper immediately passes the preceding weapon-mod row renderer to `MenuWidget_DrawList`. Reconciling its stale zero-argument function pointer to `void (*)(s32)` matches the renderer ABI and recovers the 380-byte unit without changing the retail image. |
| `0x65BB4..0x65DBC` | `psyq/libgpu/DrawOTag` | The contiguous `DrawOTag`, `PutDrawEnv`, and linked draw-environment submission routine are adjacent `libgpu/sys.c` APIs. They share the same GCC 2.8.1 `-mno-split-addresses` profile, GPU dispatch table, debug callback, draw-environment cache, and packet submit path. The combined 520-byte object remains retail-identical. |
| `0x43CE4..0x43EB8` | `audio/Sfx_DrawSlotRow` | The active-list selector directly calls the preceding renderer with the same `ItemDataRecord`, forming one 468-byte UI helper. Its recovery reconciles `Item_LookupBaseData` to its verified one-argument API, confirms renderer offsets as `kind`, `baseStats[2]`, `bonusStats[2]`, and `ammo`, and preserves retail-identical code. |
| `0x62F0..0x65AC` | `boot/Boot_InitPhase1` | The three consecutive startup-menu phases share game-state and task-node pending flags, the `-G8` profile, and the scene-load lifecycle. Their merge resolves a conflicting declaration: `Render_BeginSceneLoad` returns `int` in its definition, and every phase intentionally ignores it. The recovered 700-byte boot controller is retail-identical. |
| `0x703C0..0x703F0` | `cdrom/CdRom_SetPollCallback` | Four consecutive callback setters write the poll, sync, ready, and dispatch fields of one `DsCallbackSetPage` at `0x800A0000`. Their common internal header and identical 12-byte stores recover a 48-byte CD-ROM callback API object that remains retail-identical. |
| `0x70408..0x70464` | `cdrom/CdRom_GetLastCmd` | Six consecutive accessors expose one CD-ROM command-state page: last command, mode, current position, retry count, parameter, and seek state. `CdRom_GetCurrentPosPtr` binds that page to global `$v0`; moving the binding to the shared preamble keeps all preceding getters byte-identical, confirming the 92-byte TU. |
| `0x70464..0x704AC` | `cdrom/CdRom_Sync` | The paired CD-ROM adapters issue the corresponding Psy-Q `CD_sync` and `CD_ready` calls with a fixed mode of one. Their contiguous 72-byte object retains the symmetric ABI and is retail-identical. |
| `0x71130..0x71150` | `cdrom/CdRom_EnableDsReadSystem` | The DS-read enable setter and its adjacent state query share `CdRomSystemState::enabled`. Their common `$at`/`$v0` page bindings preserve the complete 32-byte control unit and `make check` is retail-identical. |
| `0x5223C..0x52434` | `gpu/Draw_WipeBar` | The rectangle and polygon wipe emitters share the text-cursor vertex buffer, identical bounds check, and final `Draw_EmitWipeBar` packet emission. The combined `-G8` object preserves all 504 bytes exactly; `make check` is retail-identical. |
| `0x4188..0x4A28` | `main/Camera_EntityTracking` | The angle, snap-to-entity, and zoom tracking commands share the active entity, target-list traversal, task-state cache, heading solver, velocity state, and continuation cursor. Reconciling `D_8009D254` as a `GlobalPointerSlot` establishes it as the current-entity slot; the complete 2208-byte `-G8` object remains retail-identical. |
| `0x44AA0..0x44D20` | `item/Inv_FilteredList` | The paired filtered active-list builders traverse the same item-id list, item-data tables, packed output buffer, and resulting count. The exclusion variant adds only its `tailCount` and index predicates. The unified `-G8` object preserves all 640 bytes exactly; `make check` is retail-identical. |
| `0x4CC98..0x4CE88` | `menu/Menu_RunFrame` | The argument and default entry points implement the same menu-frame scheduler: advance deferred events, card polling, widgets, presentation, error-sound recovery, and result-window transition. They differ only in the initial active-list target and recover one 496-byte `-G8` object exactly; `make check` is retail-identical. |
| `0x4090C..0x40A04` | `menu/Menu_DrawMemCardSlotLists` | The three save-slot list drawers share the menu widget selector, list renderer, text style, and sprite cursor path. Their descending sprite counts reflect slot-list depth; the complete 248-byte `--use-comm-section -G8` object is retail-identical. |
| `0x3D884..0x3DA7C` | `menu/Menu_MemCardDialogs` | The regular and startup entry points construct the same port-selection dialog, install its handlers and help row, activate card polling, and initialize its queue. Their only semantic difference is whether the caller supplies `g_McDialogMode`; the unified 504-byte `-G8` object is retail-identical. |
| `0x3DCC4..0x3DE90` | `menu/Menu_SaveSlotListView` | The constructor and destructor own the same save-slot widget tree, selected-slot state, deferred callback, navigation path, and current-node handoff. Their 460-byte `-G8` controller remains retail-identical. |
| `0x401A0..0x40348` | `menu/Menu_ActionSubmenus` | The usable-item, skill, and secondary action-list entry points share the active item record, restored selection, primary callback, and action-code renderer. Reconciling the record as `u8 *` preserves the 424-byte `--use-comm-section -G8` object exactly. |
| `0x74744..0x748DC` | `memcard/MemCard_WriteProtocol` | Command-byte emission and the response-size decoder share one `CardObj` write transaction, dispatch callback, command byte, and block-count result. The 408-byte GCC 2.8.1 `-mno-split-addresses` object is retail-identical. |
| `0x6EA04..0x6EA2C` | `pad/Pad_Toggles` | The paired Psy-Q entry points dispatch through the adjacent enable and disable callback slots. Their complete 40-byte GCC 2.8.1 `-mno-split-addresses` object is retail-identical. |
| `0x4C4A8..0x4C744` | `save/Save_MetadataWindow` | Eight contiguous save-metadata helpers form the complete two-window text controller: select a record or runtime window, load its text, append and delete encoded characters, and expose its cursor, active buffer, and remaining capacity. The shared `SaveMetadata` declarations preserve the exact 668-byte `-G8` object; `make check` is retail-identical. |
| `0x4A6C8..0x4A7D0` | `item/Inv_SelectionState` | The consecutive selection save and restore helpers form one `-G8` item-list state object. Unifying the recovered declarations establishes that the active and override item lists are `u16 *`, while their selectable-bit maps and saved selections are `s32` arrays. The complete 264-byte object preserves the retail code exactly; `make check` is retail-identical. |
| `0x48410..0x486CC` | `item/Inv_ActiveListController` | The discard, swap, clear, removal, and Wayne-list override paths manipulate one active `s16 *` inventory list. Reconciliation eliminates the competing integer declaration of `g_InvItemPtr`; the complete 700-byte `-G8` object preserves retail code exactly. |
| `0x4B7FC..0x4C090` | `item/Inv_CompareItemsForSort` | The primary and stat comparators, weapon and ammunition subrange sorters, and all three sort dispatchers share one active sort state. Each dispatcher installs the same `ItemDataRecord *(*)(s16)` lookup and prepares a `u16 *` item-id list; this confirms both the 100-entry Wayne-storage list and the 82-entry inventory list are `u16[]`. The recovered 2,196-byte TU preserves the compiler-required comparator parameters without `const` and remains retail-identical. |
| `0x609B4..0x60C1C` | `render/Pm_StopLowerHalf` | The paired upper and lower particle-model stop passes share the same slot-table cleanup flow, and the following wrapper invokes both in that order. The recovered 616-byte controller retains every direct call and `make check` is retail-identical. |
| `0x77F98..0x782A8` | `akao/voice_params` | The nine contiguous SPU voice-register writers form the implementation used immediately by `Akao_WriteVoiceParam`. They share the `AkaoVoiceParams` layout and consume its update flags in register-write order, recovering one 784-byte controller instead of ten artificial translation units. |
| `0x7A758..0x7BDB8` | `akao/voice_sequencer` | The sequencer step directly maps its active voice mask through the preceding `Akao_ForEachVoiceMasked`. The merged object also replaces the step's `M2C_UNK` placeholder for `Akao_SetNotePitch` with the established `AkaoTrack *` API. All 1376 bytes are retail-identical. |
| `0x72B14..0x72CB4` | `psyq/libds/dstype` | The original Psy-Q `LIBDS.LIB` member `DSTYPE` exports `DsGetDiskType` and contains the adjacent disk-type callback flow. `GD_cbsync` registers `GD_disk_kind` as its async-read callback; its prior `char[]` declaration masked that relationship. The compiled callback tail preserves 168 `.text` bytes exactly. `DsGetDiskType` remains matching asm until its shared status/result-state model also preserves the preceding 236 bytes, so the manifest still keeps the object split. |
| `0x71C14..0x72574` | `psyq/libds/dsfile` | The original Psy-Q `LIBDS.LIB` member `DSFILE` exports `DsSearchFile` and carries the diagnostic strings for the whole directory-cache flow. Public Psy-Q `libds/dsfile.c` orders `DsSearchFile`, `_cmp`, `DS_newmedia`, `DS_searchdir`, `DS_cachefile`, and `ds_read` exactly across this contiguous range. The reconstructed ISO-9660 directory-cache flow and symbols independently confirm the formerly anonymous 1028-byte assembly span; the existing compiled fragments remain separate until their shared object matches. |
| `0x76444..0x76580` | `psyq/libspu/s_si` | The exported `SpuSetIRQ` is the complete public Psy-Q `libspu/s_si.c` routine: it toggles bit `0x40` of `spucnt`, polls the result, and emits the SDK timeout diagnostics. Its current constrained assembly remains retail-identical while the manifest records the original library unit. |

## Deferred main pairs

| Range | Pair | Reason to defer |
| --- | --- | --- |
| `0x653B8..0x654B8` | `SetGraphDebug` / `SetGraphQueue` | Public Psy-Q `libgpu/sys.c` puts the adjacent functions together, but the recovered queue helper requires `-mno-split-addresses` and its callback has a narrower prototype. Reconciling the callback to the shared ABI and compiling with the required flags changes `SetGraphDebug` and produces 260 rather than 256 bytes; defer until the shared GPU callback model matches. |
| `0x6D874..0x6D9CC` | `_SpuInit` / `SpuStart` | Public Psy-Q `s_ini.c` groups them, and the ranges are contiguous, but `_SpuInit` matches only with GCC 2.8.1 plus `-mno-split-addresses` while `SpuStart` matches with GCC 2.7.2. A combined trial changes both code streams, beyond the expected object-local call relocation. |
| `0x6C744..0x6C930` | `CD_getsector2` / `CD_getsector` | Public Psy-Q `libcd/bios.c` groups the adjacent low-level sector readers, but the recovered `CD_getsector` uses global `$v0` and `$sp` register variables. Declaring them for the shared object changes the earlier `CD_getsector2` register allocation across its code stream; defer until that recovered register model is reconciled. |
| `0x40CF4..0x40D44` | `save/save_start` / `menu/Menu_TriggerSaveWrite` | The menu wrapper schedules the preceding save callback, but the callback owner uses `--use-comm-section -G8` while the UI helper has the default profile. The first starts card I/O; the second performs navigation and widget setup. Keep their cross-subsystem object boundary. |
| `0x67BD0..0x68200` | `gpu/gpu3` / `gpu/dma_transfer` | The DMA timeout helpers and transfer loop share controller state, but `gpu3` matches with the default compiler profile while the transfer block requires GCC 2.8.1 with `-mno-split-addresses`. The profile boundary changes code before any object-local call relocation, so retain separate objects. |
| `0x32D38..0x33478` | `memcard/MemCard_Controller` | The initializer, polling, close, active-port, restart, delayed-callback, and event helpers form one contiguous controller over the two 0x418-byte port records. Reconciling the false `volatile` mismatch on the delayed-callback timer lets the common object preserve every 0x740 retail bytes exactly. |
| `0x40038..0x40150` | `menu/Menu_DrawListRenderers` | The item, selectable-equip-slot, and context-action list renderers are contiguous -G8 list-drawing entry points. They share the active-widget state and `MenuWidget_DrawList`; the callback accepts several recovered callback signatures, so the original object leaves its declaration unprototyped. The complete 0x118-byte object is retail-identical apart from its expected GPREL relocations. |
| `0x40730..0x408A8` | `menu/Menu_ListCallbacks` | Nine contiguous default-profile list adapters dispatch to `MenuWidget_DrawList`, with the stat-allocation adapter and stat-base accessor in the same run. Different compiler profiles immediately bound the preceding save-slot and following icon-list objects. The complete 0x178-byte object is retail-identical aside from call and data relocations; its generic callback declaration remains unprototyped until all callback ABIs are reconciled. |
| `0x52D68..0x52E9C` | `menu/MenuWidget_Pool` | Pool initialization and allocation are contiguous operations over the same `MenuWidgetNode` free and active lists. The unified -G8 object is retail-identical across the full 0x134-byte range, aside from expected GPREL and call relocations. |
| `0x41CF8..0x41D10` | `menu/Menu_CommandResult` | Getter and clear operation are contiguous accessors for the same `D_8009D010` command-result slot under an identical -G8 profile. The 0x18-byte combined object is retail-identical except for its expected GPREL relocation. |
| `0x41764..0x41860` | `menu/Menu_DrawGridCellVariants` | Three contiguous grid-cell renderers have the same -G8 common-section profile and share their widget-index, dimming, cursor-offset, and sprite-allocation protocol. Their complete 0xFC-byte object is retail-identical apart from expected GPREL and call relocations. |
| `0x41898..0x41A44` | `battle/BattleCmd_Stack` | Allocation and rollback operate on the same `BattleCmdEntry` cursor and record payloads under the same -G8 common-section profile. Their complete 0x1AC-byte object is retail-identical apart from GPREL and call relocations. The allocator's two register constraints remain: removing them expands the object to 0x1B0 and changes 99 words, so that constraint is independent of the shared command-stack types. |

## Semantic candidates awaiting promotion

| Range | Candidate | Evidence and remaining work |
| --- | --- | --- |
| `0x62284..0x62B14` | `psyq/libc/Square_Vsprintf` | The candidate reconstructs the variadic formatter's 0x250-byte frame, format-spec template, flags, width/precision parser, and `d i u o p x X c s n` conversions. Psy-Q 4.6 and 4.7 `LIBC.LIB` export its exact `0x890`-byte object as `sprintf` and record `C:\PSX\SRC\C2\SPRINTF.C`: 514 words are retail-identical and the other 34 are solely link-time relocations for globals, local control-flow targets, and imported libc calls. Its 45-entry, 0xB4-byte switch table therefore belongs to this object. GCC 2.8.1 with `-mno-split-addresses -fno-strength-reduce -fno-cse-follow-jumps -fno-schedule-insns` produces the retail frame, exactly 545 instructions, and the retail 0x884-byte body. It still allocates the format-template copy through `t3/t0/t1/t2` where the exact object uses `a1/v0/v1/a0`, with later scheduling differences; it must remain matching asm until that source shape is reconciled. |

## Deferred room_m350 pairs

The following pairs are contiguous and directly connected, but are not merged
until their competing record or instance views are reconciled without changing
the matching object code:

| Range | Callback and controller | Reason to defer |
| --- | --- | --- |
| `0x57D4..0x5F1C` | `HomingTrailController` / `SequenceSpawner` | Two incompatible semantic views of a 28-byte pool record. |
| `0x6B7C..0x70DC` | `ActorOffsetPaletteCallback` / `FadingPairEmitter` | Callback and controller reconstruct different `Instance` transform views. |
| `0x91E8..0x9878` | `TransformedFlare` / `SweptAreaEmitter` | Competing `Instance` and `Actor` layouts require a common verified view. |
| `0x9878..0x9D10` | `AttachedCloud` / `AttachedCloudSpawner` | The callback and controller use different names and meanings for fields after the common prefix. |
| `0xA6D4..0xAB24` | `SineFadeCallback` / `ModelTransformEmitter` | Common 24-byte particle is established; merge must preserve the callback's `worldY` view at offset `0x200`. |

Every proposed merge must retain manifest order, pass its behavioral tests,
`make overlay-check OVERLAY=room_m350`, and a clean verification before it is
accepted.
